<html>
<head>
    <link href="~/Content/jquery-ui.css" rel="stylesheet" />
    <script src="~/Scripts/jquery-ui.js"></script>
    <script>
        var strs = @Html.Raw(Json.Encode(ViewBag.strs));
        var candlist = document.getElementById("candlist");
        function autocom(obj)
        {
            var ac = document.getElementById("selfwrite");
            var val = ac.value;
            var innerhtml = "";
            $.each(strs, function (i, item)
            {
                if (item.indexOf(val) > -1)
                {
                    innerhtml += '<li id="' + item + '">' + item + '</li>';
                }
            });
            if (innerhtml != "")
            {
                candlist.innerText = innerhtml;
                //candlist.style.display = "inherit";
                $("#candlist").css("display", "inherit");
            }
            candlist.style.display = "inherit";
        }

        //function blur(obj)
        //{
        //    candlist.style.display = "none";
        //}
    </script>
</head>
<body>
    <h4>This page shows two auto complete function, which first is done by myself and second done by cite a JS library.</h4>
    <table class="table">
        <tr>
            <td>Test auto complete:</td>
            <td>
                <input id="selfwrite" onkeyup="autocom(this)" @*onblur="blur(this)"*@/>
                <ul id="candlist" style="position:absolute;background-color:white;border:1px solid black;list-style:none;width:178px;display:none;">
                    <li>012</li>
                    <li>01kghd2</li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>Test auto complete:</td>
            <td>
                <input id="citejslibrary" />
                <script>
                    var strs = @Html.Raw(Json.Encode(ViewBag.strs));
                    $("#citejslibrary").autocomplete({
                        source: strs
                    });
                </script>
            </td>
        </tr>
        <tr>
            <td colspan="2">
                Auto complete can automatically input the following field:<br />
                @foreach (var str in ViewBag.strs)
                {
                    @str
                    @: , &nbsp;
                }
            </td>
        </tr>
    </table>
</body>
</html>